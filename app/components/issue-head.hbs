<div class="d-flex flex-column ml-2 my-3 bg-light  p-3 ">
  {{#if @withName}}
  <h6 class="issue-line font-weight-bold text-capitalize">{{@withName}}</h6>
  {{/if}}
  <div class="d-flex flex-row align-items-center">
  <FaIcon @icon="clock" class="mr-2"/>
  <span><span class="font-weight-bold mb-3">{{@issue.title}}</span> – {{@issue.created_at}}</span>
</div>
</div>
